export const environment = {
  // Run with ng serve:
  // apiBaseUrl: 'http://localhost:8080/api'
  // Run in container in same network as backend:
  apiBaseUrl: `http://${window.location.host}/api`
};